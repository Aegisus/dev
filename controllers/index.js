var playground = require('./play_route');
var loop = require('./loop_route');
var acc = require('./acc_route');
// var tempauth = require('./tempauth');
var proj = require('./proj_route');

module.exports.set = function(app) {
  // playground.set(app);
  // loop.set(app);
  // acc.set(app);
  // tempauth.set(app);
  // proj.set(app);
 }